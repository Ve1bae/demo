# Video Service 端到端测试报告

## 一、测试概述

| 项目 | 说明 |
|------|------|
| 服务 | video-service (视频点播微服务) |
| 测试框架 | JUnit 5 + Mockito + Spring Boot Test + MockMvc |
| 数据库 | H2 内存数据库 (MODE=MySQL, 与生产 MySQL 兼容) |
| 测试总数 | 46 个 (全部通过) |
| 测试层级 | 单元测试 (Mockito) + 集成测试 (MockMvc) + 端到端测试 (完整链路) |

---

## 二、UC-04 播放视频 (15 个测试)

### 单元测试 - VideoServiceImpl (8 个)

#### 1. 查询视频详情: 视频存在, 应回填兼容字段

**输入:**
```
videoMapper.selectById(1L) → Video{id=1, playUrl="http://localhost/video.mp4", playCount=5}
```

**操作:** `videoService.getVideoById(1L)`

**输出:**
```json
{
  "id": 1,
  "videoId": 1,
  "views": "5",
  "sources": {"720P": "http://localhost/video.mp4"},
  "defaultQuality": "720P"
}
```

**结果:** PASS

---

#### 2. 查询视频详情: 视频不存在, 返回 null

**输入:**
```
videoMapper.selectById(999L) → null
```

**操作:** `videoService.getVideoById(999L)`

**输出:** `null`

**结果:** PASS

---

#### 3. 播放量过万应格式化为"万"

**输入:**
```
videoMapper.selectById(2L) → Video{playCount=12345}
```

**操作:** `videoService.getVideoById(2L)`

**输出:** `result.getViews() == "1.2万"`

**结果:** PASS

---

#### 4. 播放量为 null 不报错

**输入:**
```
videoMapper.selectById(3L) → Video{playCount=null}
```

**操作:** `videoService.getVideoById(3L)`

**输出:** `result.getViews() == null` (不抛异常)

**结果:** PASS

---

#### 5. 无播放地址不设置 sources

**输入:**
```
videoMapper.selectById(4L) → Video{playUrl=null}
```

**操作:** `videoService.getVideoById(4L)`

**输出:** `result.getSources().isEmpty() == true`

**结果:** PASS

---

#### 6. 通知播放开始: 播放量自增

**输入:**
```
userVideoMapper.incrementPlayCount(1L) → (void)
videoMapper.selectById(1L) → Video{id=1, playCount=1}
```

**操作:** `videoService.incrementPlayCount(1L)`

**输出:**
```json
{"videoId": 1, "playCount": 1}
```

**结果:** PASS

---

#### 7. 首次点赞: liked 变 true 并 +1

**输入:**
```
userVideoMapper.findByUserIdAndVideoId(1,1) → UserVideo{liked=false}
videoMapper.selectById(1) → Video{likeCount=6}
```

**操作:** `videoService.toggleLike(1L, 1L)`

**输出:**
```json
{"liked": true, "likeCount": 6}
```

**结果:** PASS

---

#### 8. 无关系记录返回 liked/favorited=false

**输入:**
```
userVideoMapper.findByUserIdAndVideoId(9,1) → null
```

**操作:** `videoService.getUserVideoStatus(9L, 1L)`

**输出:**
```json
{"liked": false, "favorited": false, "videoId": 1}
```

**结果:** PASS

---

### 集成测试 - VideoController (5 个)

#### 9. 主成功流程: GET /api/videos/1 返回详情

**输入:**
```
HTTP GET /api/videos/1
数据库: INSERT INTO video (id=1, title='测试视频', play_url='http://localhost/video.mp4', ...)
```

**输出:**
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "playUrl": "http://localhost/video.mp4",
    "defaultQuality": "720P",
    "videoId": 1
  }
}
```

**结果:** PASS

---

#### 10. 异常流程: GET /api/videos/999 返回 404

**输入:**
```
HTTP GET /api/videos/999
```

**输出:**
```json
{"code": 404, "message": "视频不存在", "data": null}
```

**结果:** PASS

---

#### 11. 主成功流程: POST /api/videos/1/play 播放量自增

**输入:**
```
HTTP POST /api/videos/1/play
```

**输出:**
```json
{"code": 200, "data": {"videoId": 1, "playCount": 1}}
```

**结果:** PASS

---

#### 12. 异常流程: POST /api/videos/999/play 返回 404

**输入:**
```
HTTP POST /api/videos/999/play
```

**输出:**
```json
{"code": 404, "message": "视频不存在"}
```

**结果:** PASS

---

#### 13. 备选流程: 连续两次播放, 播放量累计为 2

**输入:**
```
HTTP POST /api/videos/1/play (第一次)
HTTP POST /api/videos/1/play (第二次)
```

**输出:**
```json
第一次: {"data": {"playCount": 1}}
第二次: {"data": {"playCount": 2}}
```

**结果:** PASS

---

### 端到端测试 - 完整播放流程 (2 个)

#### 14. 主成功流程: 进入详情页 → 加载播放地址 → 通知播放 → 播放量自增

**完整操作链路:**

```
步骤 1: GET /api/videos/1
    输入: videoId=1
    输出: {"code":200, "data":{"id":1, "playUrl":"http://localhost/video.mp4", "defaultQuality":"720P"}}

步骤 2: POST /api/videos/1/play
    输入: videoId=1 (Header X-User-Id 可选)
    输出: {"code":200, "data":{"videoId":1, "playCount":1}}
```

**结果:** PASS

---

#### 15. 异常流程: 访问不存在的视频, 详情和播放通知均返回 404

**完整操作链路:**

```
步骤 1: GET /api/videos/9999
    输入: videoId=9999
    输出: {"code":404, "message":"视频不存在", "data":null}

步骤 2: POST /api/videos/9999/play
    输入: videoId=9999
    输出: {"code":404, "message":"视频不存在"}
```

**结果:** PASS

---

## 三、UC-05 发送评论 (16 个测试)

### 单元测试 - CommentServiceImpl (8 个)

#### 1. 保存评论: likeCount 为 null 时初始化为 0

**输入:**
```
Comment{videoId=1, userId=1, content="测试评论", likeCount=null}
commentMapper.insert(comment) → 1
```

**操作:** `commentService.saveComment(comment)`

**输出:** `true`, `comment.getLikeCount() == 0`

**结果:** PASS

---

#### 2. 保存评论: 已有 likeCount 保持不变

**输入:**
```
Comment{likeCount=5, content="赞过的评论"}
```

**操作:** `commentService.saveComment(comment)`

**输出:** `true`, `comment.getLikeCount() == 5`

**结果:** PASS

---

#### 3. 分页查询: 回填 commentId 和用户信息

**输入:**
```
commentMapper.selectList(any()) → [Comment{id=10, videoId=1, userId=1, content="一条评论"}]
```

**操作:** `commentService.getCommentsByVideoIdWithPagination(1L, 1, 20)`

**输出:**
```json
{"total": 1, "page": 1, "pageSize": 20, "list": [{"commentId": 10}]}
```

**结果:** PASS

---

#### 4. 分页查询: 用户不存在 user 为 null

**输入:**
```
commentMapper.selectList(any()) → [Comment{userId=99, content="无用户评论"}]
```

**输出:** `list.get(0).getUsername() == null` (不抛异常)

**结果:** PASS

---

#### 5. 点赞评论: 存在则 likeCount+1

**输入:**
```
commentMapper.selectById(20) → Comment{id=20, likeCount=3}
```

**操作:** `commentService.likeComment(20L, 1L)`

**输出:** `true`, `comment.getLikeCount() == 4`

**结果:** PASS

---

#### 6. 点赞评论: 不存在返回 false

**输入:**
```
commentMapper.selectById(999) → null
```

**操作:** `commentService.likeComment(999L, 1L)`

**输出:** `false`

**结果:** PASS

---

#### 7. 删除评论: 调用 deleteById

**输入:**
```
commentMapper.deleteById(30) → 1
```

**操作:** `commentService.deleteComment(30L)`

**输出:** `true`

**结果:** PASS

---

#### 8. 删除评论: 不存在返回 false

**输入:**
```
commentMapper.deleteById(999) → 0
```

**输出:** `false`

**结果:** PASS

---

### 集成测试 - CommentController (6 个)

#### 9. 主成功流程: 向存在视频发送评论

**输入:**
```
HTTP POST /api/videos/1/comments
Content-Type: application/json
Body: {"content": "评论A", "userId": 1}
数据库: video(id=1) 存在
```

**输出:**
```json
{"code": 200, "message": "评论发布成功"}
```

**结果:** PASS

---

#### 10. 异常流程: 向不存在视频发送评论返回 404

**输入:**
```
HTTP POST /api/videos/999/comments
Body: {"content": "评论B", "userId": 1}
```

**输出:**
```json
{"code": 404, "message": "视频不存在"}
```

**结果:** PASS

---

#### 11. 异常流程: 评论内容为空返回 400

**输入:**
```
HTTP POST /api/videos/1/comments
Body: {}
```

**输出:**
```json
{"code": 400, "message": "评论内容不能为空"}
```

**结果:** PASS

---

#### 12. 异常流程: 评论内容为空白返回 400

**输入:**
```
HTTP POST /api/videos/1/comments
Body: {"content": "   ", "userId": 1}
```

**输出:**
```json
{"code": 400, "message": "评论内容不能为空"}
```

**结果:** PASS

---

#### 13. 主成功流程: 发送评论后重新加载列表

**输入:**
```
步骤 1: POST /api/videos/1/comments  Body: {"content":"刷新验证评论", "userId":1}
步骤 2: GET /api/videos/1/comments?page=1&pageSize=20
```

**输出:**
```json
步骤 1: {"code": 200}
步骤 2: {"code": 200, "data": {"total": 1, "list": [{"content": "刷新验证评论", "commentId": ...}]}}
```

**结果:** PASS

---

#### 14. 备选流程: 查询不存在视频的评论返回 404

**输入:**
```
HTTP GET /api/videos/999/comments
```

**输出:**
```json
{"code": 404, "message": "视频不存在"}
```

**结果:** PASS

---

### 端到端测试 - 完整评论流程 (2 个)

#### 15. 主成功流程: 提交评论 → 重新加载列表 → 新评论出现

**完整操作链路:**

```
步骤 1: POST /api/videos/1/comments
    输入: Body={"content":"端到端评论", "userId":1}
    输出: {"code":200, "message":"评论发布成功"}

步骤 2: GET /api/videos/1/comments?page=1&pageSize=20
    输入: 无
    输出: {"code":200, "data":{"total":1, "list":[{"content":"端到端评论", "commentId":..., "userId":1}]}}
```

**结果:** PASS

---

#### 16. 异常流程: 提交评论返回错误提示

**完整操作链路:**

```
步骤 1: POST /api/videos/9999/comments
    输入: Body={"content":"无视频", "userId":1}
    输出: {"code":404, "message":"视频不存在"}
```

**结果:** PASS

---

## 四、UC-06 发送视频弹幕 (15 个测试)

### 单元测试 - DanmakuServiceImpl (6 个)

#### 1. 发送弹幕: 设置 createdAt 并插入

**输入:**
```
Danmaku{videoUrl="http://localhost/video.mp4", content="前方高能", time=12.0, color="#ffffff", userId="1"}
danmakuMapper.insert(d) → 1
```

**操作:** `danmakuService.saveDanmaku(d)`

**输出:** `true`, `d.getCreatedAt() != null`

**结果:** PASS

---

#### 2. 发送弹幕: 插入失败返回 false

**输入:**
```
danmakuMapper.insert(d) → 0
```

**输出:** `false`

**结果:** PASS

---

#### 3. 按视频 URL 查询弹幕

**输入:**
```
danmakuMapper.selectList(any()) → [Danmaku{content="a"}, Danmaku{content="b"}]
```

**操作:** `danmakuService.getDanmakuByVideoUrl("u1")`

**输出:** `result.size() == 2`

**结果:** PASS

---

#### 4. 按用户 ID 查询弹幕

**输入:**
```
danmakuMapper.selectList(any()) → [Danmaku{content="x"}]
```

**操作:** `danmakuService.getDanmakuByUserId("1")`

**输出:** `result.size() == 1`

**结果:** PASS

---

#### 5. 删除弹幕: 存在返回 true

**输入:**
```
danmakuMapper.deleteById(1) → 1
```

**操作:** `danmakuService.deleteDanmaku(1L)`

**输出:** `true`

**结果:** PASS

---

#### 6. 删除弹幕: 不存在返回 false

**输入:**
```
danmakuMapper.deleteById(999) → 0
```

**输出:** `false`

**结果:** PASS

---

### 集成测试 - DanmakuController (7 个)

#### 7. 主成功流程: 向存在视频发送弹幕

**输入:**
```
HTTP POST /api/videos/1/danmakus
Content-Type: application/json
Body: {"content":"前方高能", "timeSeconds":15, "color":"#ff0000", "userId":1}
```

**输出:**
```json
{"code": 200, "data": "弹幕发送成功"}
```

**结果:** PASS

---

#### 8. 异常流程: 向不存在视频发送弹幕返回 404

**输入:**
```
HTTP POST /api/videos/999/danmakus
Body: {"content":"弹幕", "timeSeconds":1}
```

**输出:**
```json
{"code": 404, "message": "视频不存在"}
```

**结果:** PASS

---

#### 9. 异常流程: 弹幕内容为空返回 400

**输入:**
```
HTTP POST /api/videos/1/danmakus
Body: {}
```

**输出:**
```json
{"code": 400, "message": "弹幕内容不能为空"}
```

**结果:** PASS

---

#### 10. 备选流程: timeSeconds 以字符串传入

**输入:**
```
步骤 1: POST /api/videos/1/danmakus  Body: {"content":"字符串时间", "timeSeconds":"12.5", "userId":1}
步骤 2: GET /api/videos/1/danmakus?startTime=0&endTime=60
```

**输出:**
```json
步骤 1: {"code": 200}
步骤 2: {"data": [{"time": 12.5, "content": "字符串时间"}]}
```

**结果:** PASS

---

#### 11. 备选流程: 缺省 userId 默认 anonymous

**输入:**
```
步骤 1: POST /api/videos/1/danmakus  Body: {"content":"匿名弹幕", "timeSeconds":5}
步骤 2: GET /api/videos/1/danmakus?startTime=0&endTime=60
```

**输出:**
```json
步骤 1: {"code": 200}
步骤 2: {"data": [{"userId": "anonymous"}]}
```

**结果:** PASS

---

#### 12. 备选流程: 按时间区间过滤弹幕

**输入:**
```
步骤 1: POST /api/videos/1/danmakus  Body: {"content":"区间内", "timeSeconds":12.5, "userId":1}
步骤 2: GET /api/videos/1/danmakus?startTime=0&endTime=60
步骤 3: GET /api/videos/1/danmakus?startTime=20&endTime=60
```

**输出:**
```json
步骤 2: {"data": [{"content":"区间内"}]}  (长度 1)
步骤 3: {"data": []}  (长度 0, 区间外被过滤)
```

**结果:** PASS

---

#### 13. 异常流程: 查询不存在视频的弹幕返回 404

**输入:**
```
HTTP GET /api/videos/999/danmakus
```

**输出:**
```json
{"code": 404, "message": "视频不存在"}
```

**结果:** PASS

---

### 端到端测试 - 完整弹幕流程 (2 个)

#### 14. 主成功流程: 发送弹幕 → 查询列表 → 屏幕展示

**完整操作链路:**

```
步骤 1: POST /api/videos/1/danmakus
    输入: Body={"content":"高能预警", "timeSeconds":18.5, "color":"#00ff00", "userId":1}
    输出: {"code":200, "data":"弹幕发送成功"}

步骤 2: GET /api/videos/1/danmakus?startTime=0&endTime=60
    输入: 无
    输出: {"code":200, "data":[{"content":"高能预警", "time":18.5, "color":"#00ff00", "videoUrl":"http://localhost/video.mp4"}]}
```

**结果:** PASS

---

#### 15. 异常流程: 视频不存在, 发送弹幕返回错误提示

**完整操作链路:**

```
步骤 1: POST /api/videos/9999/danmakus
    输入: Body={"content":"无视频", "timeSeconds":1}
    输出: {"code":404, "message":"视频不存在"}
```

**结果:** PASS

---

## 五、测试汇总

| 用例 | 单元测试 | 集成测试 | 端到端测试 | 小计 |
|------|---------|---------|-----------|------|
| UC-04 播放视频 | 8 | 5 | 2 | 15 |
| UC-05 发送评论 | 8 | 6 | 2 | 16 |
| UC-06 发送弹幕 | 6 | 7 | 2 | 15 |
| **总计** | **22** | **18** | **6** | **46** |

```
Tests run: 46, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```
