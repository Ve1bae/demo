services:
  api-gateway:
    image: nginx:1.27-alpine
    container_name: microdemo-api-gateway
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
