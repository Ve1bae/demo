# 《航音视频》软件详细设计说明书

## 1 引言

### 1.1 编写目的

本文档在《航音视频》软件概要设计说明书的基础上，进一步细化系统的各功能模块、接口、异常处理和关键业务流程，旨在为开发团队（前端、后端、测试）提供明确、可执行的底层设计与实现依据，确保系统能够支撑视频分发、视频播放、评论弹幕、直播推流和直播互动等核心业务。

### 1.2 背景

开发软件系统名称：航音视频。

任务提出者：软件工程基础 2026 春季学期大作业。

开发者：5 人开发团队。

运行环境：现代浏览器 Web 端，后端基于 Spring Boot，基础设施包括 MySQL、MinIO、SRS 和 Nginx。

### 1.3 参考文档

- 《航音视频》软件开发计划书
- 《航音视频》软件需求规格说明书
- 《航音视频》软件概要设计说明书
- 《软件工程基础2026春大作业要求》

## 2 系统结构设计及子系统细化

系统采用前后端分离架构，后端按 Controller、Service、Mapper 分层组织。结合原详细设计的业务划分，本系统细化为以下子系统：

1. 视频上传子系统
2. 内容分发子系统
3. 视频播放子系统
4. 评论弹幕子系统
5. 直播互动子系统
6. 存储与基础设施支撑模块

### 2.1 后端分层类图

```mermaid
classDiagram
    class UserController
    class VideoController
    class MinioController
    class CommentController
    class DanmakuController
    class LiveRoomController
    class LiveDanmuController
    class LiveLikeController
    class UserService
    class VideoService
    class MinioService
    class CommentService
    class DanmakuService
    class LiveRoomService
    class LiveDanmuService
    class RoomLikesService
    class UserMapper
    class VideoMapper
    class CommentMapper
    class DanmakuMapper
    class LiveRoomMapper
    class LiveDanmuMapper
    class RoomLikesMapper

    UserController --> UserService
    VideoController --> VideoService
    MinioController --> MinioService
    CommentController --> CommentService
    DanmakuController --> DanmakuService
    LiveRoomController --> LiveRoomService
    LiveDanmuController --> LiveDanmuService
    LiveLikeController --> RoomLikesService
    UserService --> UserMapper
    VideoService --> VideoMapper
    CommentService --> CommentMapper
    DanmakuService --> DanmakuMapper
    LiveRoomService --> LiveRoomMapper
    LiveDanmuService --> LiveDanmuMapper
    RoomLikesService --> RoomLikesMapper
```

### 2.2 关键实体职责

| 实体 | 对应数据表 | 职责 |
| --- | --- | --- |
| User | sys_user | 表示系统用户，保存账号、昵称、头像等基础信息。 |
| Video | video | 表示视频内容，保存标题、描述、播放地址、封面、统计数据。 |
| Comment | comment | 表示视频评论，支持父评论形成回复关系。 |
| Danmaku | danmaku | 表示视频弹幕，保存内容、颜色、发送时间点。 |
| LiveRoom | live_room | 表示直播间，保存标题、推流地址、播放地址、状态。 |
| LiveDanmu | live_danmu | 表示直播弹幕历史记录。 |
| RoomLikes | room_likes | 表示直播间累计点赞数。 |
| UserVideo | user_video | 表示用户和视频之间的点赞、收藏关系。 |

## 3 视频上传子系统详细设计

### 3.1 模块职责

视频上传子系统负责接收前端上传的视频文件，将文件保存到 MinIO，并将视频标题、描述、封面、播放地址等元数据写入 MySQL。该模块对应需求 REQ-02。

### 3.2 活动图

```mermaid
flowchart TD
    A([开始]) --> B[选择视频文件]
    B --> C{文件格式和大小是否符合要求}
    C -- 否 --> D[提示格式或大小不符合]
    C -- 是 --> E[上传文件到MinIO]
    E --> F{上传是否成功}
    F -- 否 --> G[提示上传失败并允许重试]
    F -- 是 --> H[返回视频访问URL]
    H --> I[提交标题、简介、封面、播放地址]
    I --> J[后端写入video表]
    J --> K[返回上传成功]
    K --> L([结束])
```

### 3.3 详细顺序图

```mermaid
sequenceDiagram
    participant FE as 前端上传页面
    participant MinioCtl as MinioController
    participant MinioSvc as MinioService
    participant MinIO as MinIO
    participant VideoCtl as VideoController
    participant VideoSvc as VideoService
    participant DB as MySQL

    FE->>MinioCtl: 上传视频文件
    MinioCtl->>MinioSvc: 校验并保存对象
    MinioSvc->>MinIO: 上传文件
    MinIO-->>MinioSvc: 返回对象URL
    MinioSvc-->>FE: 返回视频URL
    FE->>VideoCtl: 提交视频元数据
    VideoCtl->>VideoSvc: 创建视频记录
    VideoSvc->>DB: 插入video记录
    DB-->>VideoSvc: 保存成功
    VideoCtl-->>FE: 返回上传成功
```

## 4 内容分发与播放子系统详细设计

### 4.1 模块职责

内容分发子系统负责首页视频列表、视频详情、个人视频列表等数据查询；播放子系统负责根据播放地址加载视频文件，提供播放、暂停、快进、倍速等基础控制。该模块对应 REQ-03 和 REQ-04。

### 4.2 视频对象状态图

```mermaid
stateDiagram-v2
    [*] --> Created: 创建记录
    Created --> Uploading: 开始上传
    Uploading --> Uploaded: MinIO返回URL
    Uploading --> Failed: 上传失败
    Failed --> Uploading: 重试上传
    Uploaded --> Public: 发布成功
    Public --> Playing: 用户播放
    Playing --> Public: 播放结束
    Public --> Deleted: 创作者删除
    Deleted --> [*]
```

### 4.3 视频播放活动图

```mermaid
flowchart TD
    A([开始]) --> B[打开视频详情页]
    B --> C[请求视频详情和播放地址]
    C --> D{视频是否存在且公开}
    D -- 否 --> E[提示视频不可访问]
    D -- 是 --> F[加载播放器]
    F --> G[查询历史弹幕和评论]
    G --> H[播放视频]
    H --> I{用户是否操作播放器}
    I -- 播放/暂停 --> J[切换播放状态]
    I -- 快进/拖动 --> K[跳转到指定时间]
    I -- 倍速 --> L[切换播放速度]
    J --> H
    K --> H
    L --> H
```

### 4.4 UC-04 播放视频对象级顺序图

```mermaid
sequenceDiagram
    actor User as 用户
    participant VP as VideoPlayer.vue
    participant VC as VideoController
    participant VS as VideoServiceImpl
    participant VM as VideoMapper
    participant VH as ViewHistoryMapper
    participant UI as UserInterestMapper
    participant DB as MySQL

    User->>VP: 点击播放
    VP->>VC: GET /api/videos/{videoId}
    VC->>VC: getVideoById(videoId)
    VC->>VS: getVideoById(videoId)
    VS->>VM: selectById(videoId)
    VM->>DB: SELECT * FROM video WHERE id=?
    DB-->>VM: Video记录
    VM-->>VS: Video实体
    VS->>VS: decorateVideo(video)
    VS-->>VC: 视频详情
    VC-->>VP: 视频JSON（含sources、playUrl）
    VP->>VP: 加载视频源并播放
    VP->>VC: POST /api/videos/{videoId}/play
    VC->>VS: incrementPlayCount(videoId, userId)
    VS->>VM: incrementPlayCount(videoId)
    VM->>DB: UPDATE video SET play_count=play_count+1
    DB-->>VM: 1
    VS->>VH: insert或update ViewHistory
    VH->>DB: INSERT INTO view_history
    DB-->>VH: 1
    VS->>VS: adjustUserInterestForVideo(userId, videoId, 1)
    VS->>UI: insert/update UserInterest
    UI->>DB: INSERT INTO user_interest
    DB-->>UI: 1
    VS-->>VC: {videoId, playCount}
    VC-->>VP: 返回新播放量
```

## 5 评论弹幕子系统详细设计

### 5.1 模块职责

评论弹幕子系统负责视频评论、视频弹幕的新增、查询和展示。评论以视频为中心进行列表展示，弹幕以视频 URL 和播放时间点为核心进行展示。该模块对应 REQ-05。

### 5.2 视频弹幕活动图

```mermaid
flowchart TD
    A([开始]) --> B[用户播放视频]
    B --> C[输入弹幕内容和颜色]
    C --> D{弹幕内容是否有效}
    D -- 否 --> E[提示内容不能为空]
    D -- 是 --> F[读取当前播放时间]
    F --> G[提交弹幕内容、颜色、时间点]
    G --> H{保存是否成功}
    H -- 否 --> I[提示发送失败]
    H -- 是 --> J[播放器显示弹幕]
    J --> K([结束])
```

### 5.3 评论发布详细顺序图

```mermaid
sequenceDiagram
    participant FE as 前端评论区
    participant Ctl as CommentController
    participant Svc as CommentService
    participant Mapper as CommentMapper
    participant DB as MySQL

    FE->>Ctl: 提交videoId、userId、content
    Ctl->>Ctl: 校验参数
    Ctl->>Svc: createComment()
    Svc->>Mapper: insert(comment)
    Mapper->>DB: INSERT comment
    DB-->>Mapper: affectedRows=1
    Mapper-->>Svc: 保存成功
    Svc-->>Ctl: 返回评论对象
    Ctl-->>FE: 返回成功响应
```

### 5.4 UC-07 发送评论对象级活动图

```mermaid
flowchart TD
    A([开始]) --> B[用户点击发送评论]
    B --> C{用户是否已登录}
    C -- 否 --> D[前端提示登录]
    D --> E([结束])
    C -- 是 --> F[前端发送POST请求]
    F --> G[CommentController接收请求]
    G --> H[VideoController校验视频存在]
    H --> I{视频是否存在}
    I -- 否 --> J[返回404错误]
    J --> E
    I -- 是 --> K[构建Comment对象]
    K --> L[UserService校验用户存在]
    L --> M{用户是否存在}
    M -- 否 --> N[返回401错误]
    N --> E
    M -- 是 --> O[CommentServiceImpl保存评论]
    O --> P[CommentMapper.insert]
    P --> Q[MySQL INSERT INTO comment]
    Q --> R{保存是否成功}
    R -- 否 --> S[返回500错误]
    S --> E
    R -- 是 --> T[返回评论发布成功]
    T --> U[前端重新加载评论列表]
    U --> V[展示新评论]
    V --> E
```

### 5.5 UC-06 发送视频弹幕对象级顺序图

```mermaid
sequenceDiagram
    actor User as 用户
    participant VP as VideoPlayer.vue
    participant DC as DanmakuController
    participant VC as VideoController
    participant DS as DanmakuServiceImpl
    participant DM as DanmakuMapper
    participant VM as VideoMapper
    participant DB as MySQL

    User->>VP: 输入弹幕内容和颜色，按回车发送
    VP->>VP: 读取当前video.currentTime
    VP->>DC: POST /api/videos/{videoId}/danmakus
    DC->>VC: getVideoById(videoId)
    VC->>VM: selectById(videoId)
    VM->>DB: SELECT * FROM video WHERE id=?
    DB-->>VM: Video记录
    VM-->>VC: Video实体
    VC-->>DC: 视频存在
    DC->>DC: 构建Danmaku对象(videoUrl, content, color, time, userId)
    DC->>DS: saveDanmaku(danmaku)
    DS->>DS: setCreatedAt(LocalDateTime.now())
    DS->>DM: insert(danmaku)
    DM->>DB: INSERT INTO danmaku
    DB-->>DM: 1
    DM-->>DS: true
    DS-->>DC: true
    DC-->>VP: 返回"弹幕发送成功"
    VP->>VP: 本地立即显示弹幕动画
    VP->>VP: 按时间点排序弹幕列表
```

## 6 直播互动子系统详细设计

### 6.1 模块职责

直播互动子系统负责直播间创建、直播状态管理、直播播放地址生成、直播弹幕广播和直播点赞统计。该模块对应 REQ-06 和 REQ-07。

### 6.2 直播互动活动图

```mermaid
flowchart TD
    A([开始]) --> B[主播创建直播间]
    B --> C[系统生成推流地址和播放地址]
    C --> D[主播使用OBS推流到SRS]
    D --> E[观众进入直播间]
    E --> F{直播间是否在线}
    F -- 否 --> G[提示直播未开始或已结束]
    F -- 是 --> H[加载HTTP-FLV直播画面]
    H --> I[建立WebSocket连接]
    I --> J[观众发送弹幕或点赞]
    J --> K[系统保存并广播互动消息]
    K --> H
```

### 6.3 直播间状态图

```mermaid
stateDiagram-v2
    [*] --> Created: 创建直播间
    Created --> Offline: 等待推流
    Offline --> Live: SRS检测到推流
    Live --> Interrupted: 推流中断
    Interrupted --> Live: 恢复推流
    Live --> Closed: 主播关闭直播
    Interrupted --> Closed: 超时关闭
    Closed --> [*]
```

### 6.4 直播点赞详细顺序图

```mermaid
sequenceDiagram
    participant FE as 直播页面
    participant Ctl as LiveLikeController
    participant Svc as RoomLikesService
    participant DB as MySQL

    FE->>Ctl: POST /live/rooms/{roomId}/like
    Ctl->>Svc: increaseLike(roomId)
    Svc->>DB: INSERT或UPDATE room_likes
    DB-->>Svc: 当前点赞数
    Svc-->>Ctl: likeCount
    Ctl-->>FE: 返回最新点赞数
```

## 7 异常处理设计

| 场景 | 异常原因 | 处理方式 | 对应测试 |
| --- | --- | --- | --- |
| 登录失败 | 用户不存在或密码错误 | 返回失败响应，前端提示重新输入。 | TC-01-03 |
| 上传失败 | 文件过大、MinIO 不可用、网络中断 | 前端提示失败，允许重新上传。 | TC-02-02 |
| 视频无法播放 | 播放地址失效或文件不存在 | 播放器显示资源不可用。 | TC-04-03 |
| 弹幕发送失败 | 参数缺失或数据库写入失败 | 提示发送失败，可重试。 | TC-05-03 |
| 直播未开始 | 直播间 offline | 显示直播未开始或已结束。 | TC-06-02 |
| WebSocket 断开 | 网络异常或服务断开 | 尝试重连，失败后提示用户。 | TC-07-03 |

## 8 详细设计追溯

| 详细设计项 | 覆盖需求 | 覆盖概要设计 |
| --- | --- | --- |
| DD-01 用户登录注册流程 | REQ-01 | DES-01 |
| DD-02 视频上传活动设计 | REQ-02 | DES-02 |
| DD-03 推荐列表处理设计 | REQ-03 | DES-03 |
| DD-04 播放器状态设计 | REQ-04 | DES-04 | 4.4 UC-04播放视频对象级顺序图, VideoPlayer.vue组件 |
| DD-05 弹幕活动设计 | REQ-05 | DES-05 | 5.4 UC-05发送评论对象级活动图, 5.5 UC-06发送视频弹幕对象级顺序图 |
| DD-06 直播间状态设计 | REQ-06 | DES-06 |
| DD-07 WebSocket 广播设计 | REQ-07 | DES-07 |
| DD-08 服务启动与异常处理 | REQ-08 | DES-08 |
