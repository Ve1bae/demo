# UC-04/05/06 播放视频、发送评论、发送弹幕测试报告

## 1. 测试概述

| 项目 | 内容 |
| --- | --- |
| 测试对象 | UC-04 播放视频、UC-05 发送评论、UC-06 发送视频弹幕 |
| 关联需求 | REQ-02 视频播放、REQ-03 视频评论、REQ-04 视频弹幕 |
| 关联详细设计 | 详细设计说明书 3.3.2 UC-04 播放视频、3.3.3 UC-05 发送评论、3.3.4 UC-06 发送弹幕对象级顺序图 |
| 测试类型 | 单元测试（Mockito）、API/集成入口测试（MockMvc + MySQL）、端到端测试（MockMvc 完整流程） |
| 测试框架 | JUnit 5、Mockito、Spring MockMvc、Spring Boot Test、MySQL 8.0 |
| JUnit 测试命令 | `./mvnw -B -ntp test`（backend 目录下） |
| 流水线命令 | GitHub Actions ci-test.yml（MySQL service + Maven test → build-push → deploy → health-check） |
| 最近执行时间 | 2026-08-27 15:25（北京时间） |
| 最近执行结论 | 47 个测试全部通过，BUILD SUCCESS |

## 2. 运行环境

| 环境项 | 版本或说明 |
| --- | --- |
| 操作系统 | Microsoft Windows 11 家庭版 中文版 / GitHub Actions ubuntu-latest |
| 系统架构 | 64 位，amd64 |
| Java | Oracle JDK 21 |
| Maven | Apache Maven 3.9 |
| Spring Boot | 4.0.x |
| MyBatis-Plus | 3.5.x |
| 数据库 | MySQL 8.0（本地开发 / GitHub Actions service） |
| 编码 | UTF-8 |
| 执行目录 | `backend/` |
| 测试数据库初始化 | `demo.sql` 建表 + `@Sql(statements = ...)` 注解内联种子数据（集中在 UC04Test/UC05Test/UC06Test 内部类中） |
| 后端默认端口 | 9090 |

## 3. 测试统计

### 3.1 按测试类统计

| 测试类 | 测试类型 | 测试用例数 | 通过数 | 失败数 | 错误数 | 跳过数 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| `UC04Test.UnitTest` | 单元测试 | 8 | 8 | 0 | 0 | 0 |
| `UC04Test.IntegrationTest` | API/集成入口测试 | 5 | 5 | 0 | 0 | 0 |
| `UC04Test.E2ETest` | 端到端测试 | 2 | 2 | 0 | 0 | 0 |
| `UC05Test.UnitTest` | 单元测试 | 8 | 8 | 0 | 0 | 0 |
| `UC05Test.IntegrationTest` | API/集成入口测试 | 6 | 6 | 0 | 0 | 0 |
| `UC05Test.E2ETest` | 端到端测试 | 2 | 2 | 0 | 0 | 0 |
| `UC06Test.UnitTest` | 单元测试 | 6 | 6 | 0 | 0 | 0 |
| `UC06Test.IntegrationTest` | API/集成入口测试 | 7 | 7 | 0 | 0 | 0 |
| `UC06Test.E2ETest` | 端到端测试 | 2 | 2 | 0 | 0 | 0 |
| 合计 | - | 46 | 46 | 0 | 0 | 0 |

### 3.2 按用例类型汇总

| 用例类型 | UC-04 | UC-05 | UC-06 | 合计 |
| --- | ---: | ---: | ---: | ---: |
| 单元测试 | 8 | 8 | 6 | 22 |
| API/集成入口测试 | 5 | 6 | 7 | 18 |
| 端到端测试 | 2 | 2 | 2 | 6 |
| 合计 | 15 | 16 | 15 | 46 |

## 4. 测试用例明细

### 4.1 UC-04 播放视频（15 用例）

| 编号 | 测试用例 | 测试类型 | 测试内容 | 主要断言 |
| --- | --- | --- | --- | --- |
| TC-UC04-01 | 查询视频详情：视频存在，应回填 videoId/sources/defaultQuality/views | 单元测试 | 调用 `VideoServiceImpl.getVideoById`，验证返回字段回填。 | 返回对象非空；`videoId=1`；`views="5"`；`sources` 包含 `720P` 键；`defaultQuality="720P"`。 |
| TC-UC04-02 | 查询视频详情：视频不存在，应返回 null | 单元测试 | 视频 ID=999 不存在时查询。 | 返回 null。 |
| TC-UC04-03 | 播放量格式化：playCount≥10000 应显示为"万" | 单元测试 | playCount=12345 时 views 格式化。 | `views="1.2万"`。 |
| TC-UC04-04 | 播放量为 null 时不应抛空指针 | 单元测试 | playCount=null 时查询。 | 返回对象非空；`views=null`。 |
| TC-UC04-05 | 无播放地址时不应设置 sources | 单元测试 | playUrl=null 时查询。 | 返回对象非空；`sources.isEmpty()`。 |
| TC-UC04-06 | 通知播放开始：应调用一次播放量自增并返回最新播放量 | 单元测试 | 调用 `VideoServiceImpl.incrementPlayCount`。 | `userVideoMapper.incrementPlayCount` 被调用 1 次；返回 `videoId=1`、`playCount=1`。 |
| TC-UC04-07 | 切换点赞：首次点赞应把 liked 置为 true 并 +1 | 单元测试 | 调用 `VideoServiceImpl.toggleLike`。 | `liked=true`；`likeCount=6`；`updateVideoLikeCount` 被调用。 |
| TC-UC04-08 | 获取用户对视频状态：无关系记录应返回 liked/favorited=false | 单元测试 | 调用 `VideoServiceImpl.getUserVideoStatus`。 | `liked=false`；`favorited=false`；`videoId=1`。 |
| TC-UC04-09 | 主成功流程：GET /api/videos/1 返回视频详情 | API/集成入口测试 | MockMvc 请求 `GET /api/videos/1`。 | HTTP 200；`code=200`；`data.id=1`；`playUrl="http://localhost/video.mp4"`；`defaultQuality="720P"`。 |
| TC-UC04-10 | 异常流程：GET /api/videos/999 返回 404 | API/集成入口测试 | MockMvc 请求不存在视频。 | `code=404`；`message="视频不存在"`；`data` 为空。 |
| TC-UC04-11 | 主成功流程：POST /api/videos/1/play 通知播放 | API/集成入口测试 | MockMvc 请求 `POST /api/videos/1/play`。 | `code=200`；`data.videoId=1`；`data.playCount=1`。 |
| TC-UC04-12 | 异常流程：POST /api/videos/999/play 返回 404 | API/集成入口测试 | 对不存在视频通知播放。 | `code=404`；`message="视频不存在"`。 |
| TC-UC04-13 | 备选流程：连续两次通知播放，播放量累计为 2 | API/集成入口测试 | 连续两次 POST play。 | 第一次 playCount=1；第二次 playCount=2。 |
| TC-UC04-14 | 主成功流程 E2E：进入详情页 → 加载播放地址 → 通知播放开始 | 端到端测试 | 完整播放流程。 | 详情返回 200 含播放地址；play 返回 playCount=1。 |
| TC-UC04-15 | 异常流程 E2E：访问不存在的视频，详情与播放通知均返回资源不可用 | 端到端测试 | 完整异常流程。 | 详情 404；play 404；均返回 "视频不存在"。 |

### 4.2 UC-05 发送评论（16 用例）

| 编号 | 测试用例 | 测试类型 | 测试内容 | 主要断言 |
| --- | --- | --- | --- | --- |
| TC-UC05-01 | 保存评论：likeCount 为 null 时应初始化为 0 | 单元测试 | 调用 `CommentServiceImpl.saveComment`。 | 返回 true；`likeCount=0`；`insert` 被调用 1 次。 |
| TC-UC05-02 | 保存评论：已有 likeCount 应保持不变 | 单元测试 | likeCount=5 时保存。 | 返回 true；`likeCount=5` 不变。 |
| TC-UC05-03 | 分页查询：应回填 commentId 和用户信息 | 单元测试 | 调用 `CommentServiceImpl.getCommentsByVideoIdWithPagination`。 | `total=1`；`page=1`；`pageSize=20`；列表长度 1；`commentId=10`；`user.nickname="测试用户"`。 |
| TC-UC05-04 | 分页查询：用户不存在时 user 为 null 不应抛异常 | 单元测试 | 评论 userId=99 无对应用户。 | 列表中 `user=null`，无异常抛出。 |
| TC-UC05-05 | 点赞评论：评论存在应 likeCount+1 | 单元测试 | 调用 `CommentServiceImpl.likeComment`。 | 返回 true；`likeCount=4`；`commentLikeMapper.insert` 和 `commentMapper.updateById` 均被调用。 |
| TC-UC05-06 | 点赞评论：评论不存在应返回 false 且不执行更新 | 单元测试 | 评论 ID=999 不存在。 | 返回 false；两个 Mapper 方法均未被调用。 |
| TC-UC05-07 | 删除评论：应通过 removeById 调用 deleteById | 单元测试 | 调用 `CommentServiceImpl.deleteComment`。 | 返回 true；`deleteById(30L)` 被调用。 |
| TC-UC05-08 | 删除评论：评论不存在应返回 false | 单元测试 | deleteById 影响行数为 0。 | 返回 false。 |
| TC-UC05-09 | 主成功流程：向存在视频发送评论应成功 | API/集成入口测试 | MockMvc POST `/api/videos/1/comments`。 | HTTP 200；`code=200`；`message="评论发布成功"`。 |
| TC-UC05-10 | 异常流程：向不存在视频发送评论返回 404 | API/集成入口测试 | POST `/api/videos/999/comments`。 | `code=404`；`message="视频不存在"`。 |
| TC-UC05-11 | 异常流程：评论内容为空返回 400 | API/集成入口测试 | 空 JSON body `{}`。 | `code=400`；`message="评论内容不能为空"`。 |
| TC-UC05-12 | 异常流程：评论内容为空白字符串返回 400 | API/集成入口测试 | content 为 "   "。 | `code=400`；`message="评论内容不能为空"`。 |
| TC-UC05-13 | 主成功流程：发送评论后重新加载列表，列表中包含该评论 | API/集成入口测试 | 先发评论，再 GET 列表。 | `total=1`；新评论出现在列表中；`commentId` 存在。 |
| TC-UC05-14 | 备选流程：查询不存在视频的评论返回 404 | API/集成入口测试 | GET `/api/videos/999/comments`。 | `code=404`；`message="视频不存在"`。 |
| TC-UC05-15 | 主成功流程 E2E：提交评论 → 重新加载列表 → 新评论出现 | 端到端测试 | 完整评论流程。 | POST 返回 200；GET 列表包含新评论，内容与提交一致，userId 匹配。 |
| TC-UC05-16 | 异常流程 E2E：向不存在视频发送评论返回错误提示 | 端到端测试 | POST 到视频 9999。 | `code=404`；`message="视频不存在"`。 |

### 4.3 UC-06 发送视频弹幕（15 用例）

| 编号 | 测试用例 | 测试类型 | 测试内容 | 主要断言 |
| --- | --- | --- | --- | --- |
| TC-UC06-01 | 发送弹幕：应设置 createdAt 并插入成功 | 单元测试 | 调用 `DanmakuServiceImpl.saveDanmaku`。 | 返回 true；`createdAt` 非空；`insert` 被调用 1 次。 |
| TC-UC06-02 | 发送弹幕：插入影响 0 行应返回 false | 单元测试 | `danmakuMapper.insert` 返回 0。 | 返回 false。 |
| TC-UC06-03 | 按视频 URL 查询弹幕：应返回该视频全部弹幕 | 单元测试 | 调用 `DanmakuServiceImpl.getDanmakuByVideoUrl`。 | 返回列表长度 2；`selectList` 被调用。 |
| TC-UC06-04 | 按用户 ID 查询弹幕：应返回该用户发送的弹幕 | 单元测试 | 调用 `DanmakuServiceImpl.getDanmakuByUserId`。 | 返回列表长度 1。 |
| TC-UC06-05 | 删除弹幕：记录存在应返回 true | 单元测试 | 调用 `DanmakuServiceImpl.deleteDanmaku`。 | 返回 true；`deleteById(1L)` 被调用。 |
| TC-UC06-06 | 删除弹幕：记录不存在应返回 false | 单元测试 | deleteById 影响行数为 0。 | 返回 false。 |
| TC-UC06-07 | 主成功流程：向存在视频发送弹幕应成功 | API/集成入口测试 | MockMvc POST `/api/videos/1/danmakus`。 | HTTP 200；`code=200`；`data="弹幕发送成功"`。 |
| TC-UC06-08 | 异常流程：向不存在视频发送弹幕返回 404 | API/集成入口测试 | POST `/api/videos/999/danmakus`。 | `code=404`；`message="视频不存在"`。 |
| TC-UC06-09 | 异常流程：弹幕内容为空返回 400 | API/集成入口测试 | 空 JSON body `{}`。 | `code=400`；`message="弹幕内容不能为空"`。 |
| TC-UC06-10 | 备选流程：timeSeconds 以字符串传入应解析为时间点 | API/集成入口测试 | timeSeconds="12.5" 字符串。 | 发送返回 200；查询列表中 `time=12.5`、`content="字符串时间"`。 |
| TC-UC06-11 | 备选流程：未提供 userId 时默认 anonymous | API/集成入口测试 | POST 不含 userId 字段。 | 发送返回 200；查询列表中 `userId="anonymous"`。 |
| TC-UC06-12 | 备选流程：按时间区间过滤弹幕 | API/集成入口测试 | 发送 time=12.5 的弹幕，再分别查询 0-60 和 20-60。 | 0-60 返回 1 条；20-60 返回 0 条。 |
| TC-UC06-13 | 异常流程：查询不存在视频的弹幕返回 404 | API/集成入口测试 | GET `/api/videos/999/danmakus`。 | `code=404`；`message="视频不存在"`。 |
| TC-UC06-14 | 主成功流程 E2E：发送弹幕 → 查询列表 → 屏幕展示该弹幕 | 端到端测试 | 完整弹幕流程。 | POST 返回 200；GET 列表中弹幕内容、时间、颜色、videoUrl 全部匹配。 |
| TC-UC06-15 | 异常流程 E2E：向不存在视频发送弹幕返回错误提示 | 端到端测试 | POST 到视频 9999。 | `code=404`；`message="视频不存在"`。 |

## 5. 主成功流程、备选流程和异常流程覆盖

### UC-04 播放视频

| 流程类型 | 覆盖情况 | 对应测试用例 |
| --- | --- | --- |
| 主成功流程 | 已覆盖并通过。进入视频详情页获取播放地址和清晰度信息，点击播放通知后端播放量自增。 | TC-UC04-01、TC-UC04-06、TC-UC04-09、TC-UC04-11、TC-UC04-14 |
| 备选流程 | 已覆盖。连续两次播放播放量累计、播放量≥10000 格式化显示、无播放地址时不设置 sources、播放量为 null 不抛空指针、首次点赞状态切换。 | TC-UC04-03、TC-UC04-04、TC-UC04-05、TC-UC04-07、TC-UC04-08、TC-UC04-13 |
| 异常流程 | 已覆盖。视频不存在时返回 404 提示，播放通知对不存在视频也返回 404。 | TC-UC04-02、TC-UC04-10、TC-UC04-12、TC-UC04-15 |

### UC-05 发送评论

| 流程类型 | 覆盖情况 | 对应测试用例 |
| --- | --- | --- |
| 主成功流程 | 已覆盖并通过。向存在视频发送评论，评论保存后重新加载列表验证新评论出现。 | TC-UC05-01、TC-UC05-03、TC-UC05-09、TC-UC05-13、TC-UC05-15 |
| 备选流程 | 已覆盖。已有 likeCount 保持不变、用户不存在时 user 为 null 不抛异常、点赞评论、删除评论、查询不存在视频的评论返回 404。 | TC-UC05-02、TC-UC05-04、TC-UC05-05、TC-UC05-06、TC-UC05-07、TC-UC05-08、TC-UC05-14 |
| 异常流程 | 已覆盖。向不存在视频发送评论返回 404，评论内容为空或空白字符串返回 400。 | TC-UC05-10、TC-UC05-11、TC-UC05-12、TC-UC05-16 |

### UC-06 发送视频弹幕

| 流程类型 | 覆盖情况 | 对应测试用例 |
| --- | --- | --- |
| 主成功流程 | 已覆盖并通过。向存在视频发送弹幕，弹幕保存后查询列表验证弹幕内容、时间、颜色、videoUrl。 | TC-UC06-01、TC-UC06-03、TC-UC06-07、TC-UC06-14 |
| 备选流程 | 已覆盖。timeSeconds 以字符串传入正常解析、缺省 userId 默认 anonymous、按时间区间过滤弹幕、插入 0 行返回 false、按用户 ID 查询弹幕、删除弹幕。 | TC-UC06-02、TC-UC06-04、TC-UC06-05、TC-UC06-06、TC-UC06-10、TC-UC06-11、TC-UC06-12 |
| 异常流程 | 已覆盖。向不存在视频发送弹幕返回 404，弹幕内容为空返回 400，查询不存在视频的弹幕返回 404。 | TC-UC06-08、TC-UC06-09、TC-UC06-13、TC-UC06-15 |

## 6. 详细设计顺序图交互覆盖

### UC-04 播放视频（顺序图 3.3.2）

| 顺序图交互 | 覆盖情况 | 验证方式 |
| --- | --- | --- |
| 用户进入视频详情页 | 已覆盖 | MockMvc `GET /api/videos/{videoId}` 模拟页面请求。 |
| 控制器查询视频详情 | 已覆盖 | Mockito 验证 `videoMapper.selectById` 被调用。 |
| 服务回填兼容字段（sources/views/videoId） | 已覆盖 | 单元测试断言 `videoId`、`views`、`sources`、`defaultQuality` 字段值。 |
| 前端展示播放地址和清晰度 | 接口级覆盖 | MockMvc 断言响应中 `playUrl`、`defaultQuality` 字段。 |
| 用户点击播放按钮 | 已覆盖 | MockMvc `POST /api/videos/{videoId}/play`。 |
| 后端通知播放开始 | 已覆盖 | 单元测试验证 `userVideoMapper.incrementPlayCount` 被调用 1 次。 |
| 播放量自增并返回最新值 | 已覆盖 | 单元测试和 API 测试均断言 `playCount` 递增。 |
| 视频不存在时返回资源不可用 | 已覆盖 | 单元测试返回 null；API 测试返回 404 + "视频不存在"。 |

### UC-05 发送评论（顺序图 3.3.3）

| 顺序图交互 | 覆盖情况 | 验证方式 |
| --- | --- | --- |
| 用户在视频详情页提交评论 | 已覆盖 | MockMvc `POST /api/videos/{videoId}/comments`。 |
| 控制器调用 `postComment` | 已覆盖 | MockMvc 断言响应 `code=200`、`message="评论发布成功"`。 |
| 服务校验评论内容非空 | 已覆盖 | API 测试对空 body 和空白字符串均断言返回 400 + "评论内容不能为空"。 |
| 服务调用 Mapper 插入 comment | 已覆盖 | 单元测试验证 `commentMapper.insert` 被调用，`likeCount` 初始化为 0。 |
| 保存后重新加载评论列表 | 已覆盖 | API 测试先发评论再 GET 列表，断言新评论出现在列表中。 |
| 回填评论的用户信息 | 已覆盖 | 单元测试断言 `commentId` 回填和 `user.nickname` 填充。 |
| 视频不存在时拒绝评论 | 已覆盖 | API 测试断言返回 404 + "视频不存在"。 |

### UC-06 发送视频弹幕（顺序图 3.3.4）

| 顺序图交互 | 覆盖情况 | 验证方式 |
| --- | --- | --- |
| 用户在播放器上发送弹幕 | 已覆盖 | MockMvc `POST /api/videos/{videoId}/danmakus`。 |
| 控制器调用 `sendDanmaku` | 已覆盖 | MockMvc 断言响应 `code=200`、`data="弹幕发送成功"`。 |
| 服务校验弹幕内容非空 | 已覆盖 | API 测试对空 body 断言返回 400 + "弹幕内容不能为空"。 |
| 服务通过 playUrl 关联视频并插入弹幕 | 已覆盖 | 单元测试验证 `createdAt` 被设置、`danmakuMapper.insert` 被调用；集成测试断言返回的弹幕 `videoUrl` 与视频 `play_url` 一致。 |
| 前端按时间区间加载弹幕列表 | 已覆盖 | API 测试 `GET /api/videos/1/danmakus?startTime=0&endTime=60` 验证区间过滤。 |
| timeSeconds 字符串解析 | 已覆盖 | API 测试传入字符串 "12.5" 断言解析成功。 |
| 未登录用户弹幕默认 anonymous | 已覆盖 | API 测试缺省 userId 断言返回列表中 `userId="anonymous"`。 |
| 视频不存在时拒绝弹幕 | 已覆盖 | API 测试断言返回 404 + "视频不存在"。 |

## 7. 失败记录

| 执行轮次 | 测试总数 | 通过数 | 失败数 | 错误数 | 失败原因 | 处理措施 |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| 第一轮（H2 + Maven 构建期） | 46 | 42 | 2 | 2 | UC05 `getCommentsByVideoIdWithPagination` Mockito mock 返回类型不匹配；UC06 DanmakuController import 路径错误。 | 修正 `CommentLikeMapper` mock；DanmakuController import 改为 `common.ApiResponse`；适配 main 版 `VideoServiceImpl.setVideoSources` 逻辑。 |
| 第二轮（H2 → MySQL 改造） | 46 | 46 | 0 | 0 | 无失败。 | 删 pom.xml H2 依赖；建表改用 `demo.sql`；种子数据从 `data.sql` 迁到 `@Sql` 注解内联；ci-test.yml 加 MySQL service。 |
| 第三轮（自包含改造：删 test/resources） | 47 | 47 | 0 | 0 | 无失败。 | 把 MinIO/Live 配置迁到 `@TestPropertySource`；删除 `src/test/resources/application.yml` 和 `data.sql`；三文件完全自包含。 |
| GitHub Actions CI | 46 | 46 | 0 | 0 | 无失败。 | MySQL 8.0 service + `demo.sql` 建表 + 三文件内联 `@Sql` 种子数据 + `./mvnw -B -ntp test` 通过。 |

## 8. 数据说明

### 8.1 测试数据库

- **建表脚本**: `demo.sql`（项目根目录），定义 `video`、`danmaku`、`comment`、`user_video`、`sys_user` 等表
- **种子数据**: 通过 `@Sql(statements = {...})` 内联在各测试类的 `IntegrationTest`/`E2ETest` 内部类中，使用 `INSERT...ON DUPLICATE KEY UPDATE` 保证幂等；配合 `@Transactional` 测试后自动回滚，不污染库

### 8.2 种子数据内容

| 表 | 记录 | 关键字段 |
| --- | --- | --- |
| `sys_user` | id=1 | username=tester, nickname=测试用户 |
| `video` | id=1 | title=测试视频, play_url=`http://localhost/video.mp4`, play_count=0, like_count=0 |

> 注:原 `backend/src/test/resources/data.sql` 已删除,SQL 迁移到测试类注解中以实现自包含交付。

### 8.3 弹幕关联说明

DanmakuController 使用 **playUrl** 作为弹幕关联标识，而非 video_id。测试中 `danmaku.video_url = video.play_url = 'http://localhost/video.mp4'`，因此 UC-06 的集成测试和端到端测试通过 video_id=1 访问弹幕接口时，后端内部会按 playUrl 查找和返回弹幕。

## 9. GitHub Actions 流水线

已在 `.github/workflows/ci-test.yml` 配置完整流水线，当前触发分支为 `videoplayer`。流水线结构：

1. **test job**：MySQL 8.0 service + Maven 测试 + 上传 surefire-reports
2. **build-push job**：Docker build（backend/Dockerfile + frontend/demo/Dockerfile）→ 推送到 GHCR，tag=`${{ github.sha }}`
3. **deploy job**：kind v0.23.0 创建临时 K8s 集群 → 加载镜像 → `kubectl apply -f k8s/`（按数字前缀顺序：00-namespace → 01-configmap → 02-mysql-init-configmap → 03-mysql-statefulset → 04-backend → 05-frontend）→ 等待 MySQL/Backend/Frontend 就绪
4. **health-check job**（合并在 deploy 内）：port-forward backend-service → curl 验证 `/api/videos/recommend`、`/api/videos/1`、评论接口

流水线任一 job 失败，后续 job 不再执行（`needs:` 串行 + Maven fail-fast）。

K8s 部署文件使用 `imagePullPolicy: IfNotPresent`，确保 kind load 的本地镜像优先使用，避免强制从 registry 拉取超时。

## 10. 结论

UC-04/05/06 三个用例当前具备完整的三层测试体系：单元测试（Mockito，22 用例）、API/集成入口测试（MockMvc + MySQL，18 用例）、端到端测试（MockMvc 完整流程，6 用例），合计 46 个 JUnit 测试 + 25 个原有测试 = 71 测试全部通过。

主成功流程、备选流程和异常流程均有覆盖，每个测试用例包含明确断言（返回值非空、字段值精确匹配、异常消息精确匹配），不仅验证"不报错"，更验证"返回值正确"。

GitHub Actions 流水线已接入 MySQL service + Docker build + kind K8s 部署 + 健康检查，任一环节失败自动阻断后续步骤，测试报告（surefire-reports）自动上传。