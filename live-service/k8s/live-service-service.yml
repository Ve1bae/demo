apiVersion: v1
kind: Service
metadata:
  name: live-service
  labels:
    app.kubernetes.io/name: live-service
    app.kubernetes.io/part-of: hangyin
spec:
  selector:
    app.kubernetes.io/name: live-service
  ports:
    - name: http
      port: 8090
      targetPort: http
