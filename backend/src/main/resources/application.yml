spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hangyin_video?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: liyuhao2006.

  servlet:
    multipart:
      max-file-size: 2GB
      max-request-size: 2GB

server:
  port: 9090

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: hangyin-video

video:
  chunk-dir: C:/hangyin/chunks
