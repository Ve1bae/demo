spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hangyin_video?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: mkj162829
  servlet:
    multipart:
      max-file-size: 2GB
      max-request-size: 2GB

app:
  upload-dir: backend/uploads

live:
  srs:
    rtmp-base-url: ${LIVE_RTMP_BASE_URL:rtmp://${APP_HOST:10.213.10.6}/live}
    http-base-url: ${LIVE_HTTP_BASE_URL:http://${APP_HOST:10.213.10.6}:8081}

minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  public-base-url: ${MINIO_PUBLIC_BASE_URL:http://${APP_HOST:10.213.10.6}:8082/video}
  access-key: minioadmin
  secret-key: minioadmin
  bucket: hangyin-video

video:
  chunk-dir: C:/hangyin/chunks
