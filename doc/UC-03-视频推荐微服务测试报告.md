# UC-03 浏览推荐视频微服务测试报告

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 测试对象 | `video-service` 中的 UC-03 浏览推荐视频功能 |
| 需求编号 | `REQ-02` |
| 用例编号 | `UC-03` |
| 测试分支 | `microservice` |
| 测试日期 | 2026-09-03 |
| 被测接口 | `GET /api/videos/recommend` |
| 健康检查 | `GET /actuator/health` |
| 报告状态 | 三个服务测试、UC-03 真实 HTTP/浏览器回归、Compose 联调、故障演练以及单体/微服务性能测试已完成；SRS 本机专项保留为未完成项 |

## 2. 测试依据与目标

测试依据为《软件工程基础实践》课程任务书、软件需求规格说明书中的 `REQ-02` 和 `UC-03`，以及 UC-03 系统级、组件级和对象级交互设计。

本轮测试目标如下：

1. 验证推荐服务中的筛选、打分、排序、分页和异常分支。
2. 验证 Controller 的参数绑定、请求头处理和响应格式。
3. 验证 Spring 容器、JDBC 仓储与数据库之间的集成。
4. 验证制成镜像后的服务能够连接真实 MySQL、通过健康检查并响应真实 HTTP 请求。
5. 验证用户从前端首页执行推荐浏览、筛选、登录推荐、空结果和进入播放页的完整流程。
6. 为流水线保留可判断的断言、原始输入和实际输出；任一测试失败时停止后续步骤。

## 3. 测试范围

### 3.1 已测试

- 游客读取公开视频。
- 登录用户携带 `X-User-Id` 后读取观看历史。
- 关注作者、兴趣标签和已观看视频对推荐分数的影响。
- 分类和关键词组合筛选。
- 推荐排序、分页、空结果和边界参数修正。
- 非数字页码、非数字用户标识等异常输入。
- 标签解析、视频播放地址和作者关注状态的响应字段补充。
- H2 建表、初始化数据和 MyBatis 查询。
- 完整服务与 UC-03 推荐核心的 Spring Boot 测试。

### 3.2 当前已验证

- 真实 user-service 偏好调用已完成联调；user-service 停止时推荐降级为游客/热度排序。
- 前端通过网关访问微服务的 UC-03 Playwright 已执行并通过 `5/5`。
- Docker 真实 MySQL 推荐 API 已执行并通过 `8/8`；视频互动 HTTP E2E 已通过 `3/3`。
- Compose 联调、Kubernetes 健康检查、故障处理以及单体/微服务 API 性能已有独立证据；SRS 浏览器专项因本机缺少 FFmpeg 未通过。

## 4. 测试环境

| 项目 | 配置 |
| --- | --- |
| 操作系统 | Windows，本地开发环境 |
| Java | 本地测试 21.0.11；容器运行时 21.0.12 |
| Spring Boot | 4.0.6 |
| Maven | 3.9.x |
| Node.js | 24.15.0，用于真实 API 测试脚本 |
| Docker Engine | 29.3.1 |
| 数据库 | H2 内存数据库 MySQL 模式；MySQL 8.4.11 容器 |
| 浏览器测试 | Playwright 1.62.1、Chromium、Vue/Vite |
| 服务端口 | `8082` |
| MySQL 测试端口 | 主机 `3308` 映射到容器 `3306` |
| E2E 端口 | 前端 `5173`；单体后端 `8080`；临时 MySQL 主机端口 `3307` |
| Docker 镜像 | `hangyin/video-service:ci`；整合分支本地构建 |

数据库密码不写入代码或报告，由环境变量 `VIDEO_SERVICE_DB_PASSWORD` 或 GitHub Secret 注入。

## 5. 测试策略与通过标准

| 层级 | 测试方式 | 判断方式 | 通过标准 |
| --- | --- | --- | --- |
| 完整服务单元/API 测试 | JUnit、Mockito、MockMvc | 对完整服务已有行为执行断言 | 10 项全部通过 |
| UC-03 推荐单元测试 | JUnit、Mockito | 对排序、个性化、分页和字段执行断言 | 8 项全部通过 |
| UC-03 H2 API 测试 | Spring 容器、H2、MyBatis、MockMvc | 对数据库查询后的 HTTP 响应执行断言 | 6 项全部通过 |
| 真实 API 测试 | Docker、MySQL 8.4、打包后的服务、Node.js `fetch` | 8 项断言全部通过 | 通过 |
| 网关/前端 UC-03 浏览器 E2E | Playwright、Chromium、微服务 Compose | 5 项全部通过 | 通过 |

进入测试的条件：代码能够编译，数据库脚本可读取，Docker Engine 可用。

退出测试的条件：测试总数、通过数、失败数和失败原因可追溯；所有已纳入本轮范围的测试通过；原始报告可获取。当前 UC-03 网关/前端 E2E 已接通并计入本轮结果。

## 6. 自动化测试结果

| 测试类型 | 总数 | 通过 | 失败 | 错误 | 跳过 | 通过率 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 完整服务现有测试 | 10 | 10 | 0 | 0 | 0 | 100% |
| UC-03 推荐单元测试 | 8 | 8 | 0 | 0 | 0 | 100% |
| UC-03 H2 API 集成测试 | 6 | 6 | 0 | 0 | 0 | 100% |
| 真实 MySQL API 测试 | 8 | 8 | 0 | 0 | 0 | 100% |
| 网关/前端 UC-03 Playwright E2E | 5 | 5 | 0 | 0 | 0 | 100% |
| **video-service Maven 合计** | **24** | **24** | **0** | **0** | **0** | **100%** |

JUnit 当前结果：`Tests run: 24, Failures: 0, Errors: 0, Skipped: 0`，构建结果为 `BUILD SUCCESS`。

真实 API 和 Playwright 已在当前 `microservice` 分支的本地 Compose 环境重新执行；旧分支结果仅作为历史背景，不作为当前通过依据。

## 7. 单元与 H2 API 测试明细

### 7.1 推荐服务单元测试

测试文件：`video-service/src/test/java/com/example/video/recommendation/RecommendationRankingTest.java`

| 编号范围 | 覆盖内容 | 数量 | 结果 |
| --- | --- | ---: | --- |
| `UNIT-TC-03-01` 至 `03-03` | 热度排序、关键词筛选、分页参数修正 | 3 | 通过 |
| `UNIT-TC-03-04` 至 `03-09` | 关注作者、越界页、游客、登录用户、兴趣、观看历史 | 6 | 通过 |
| `UNIT-TC-03-10` 至 `03-14` | 分类传递、时间排序、空字段、标签处理、页大小上限 | 5 | 通过 |
| `UNIT-TC-03-15` 至 `03-18` | 最大页码、空时间、空白关键词、最大计数值 | 4 | 通过 |

### 7.2 完整服务现有测试

测试文件：`video-service/src/test/java/com/example/video/VideoServiceUnitTest.java`、`video-service/src/test/java/com/example/video/VideoApiIntegrationTest.java`

| 编号范围 | 覆盖内容 | 数量 | 结果 |
| --- | --- | ---: | --- |
| `VIDEO-UNIT-01` 至 `VIDEO-UNIT-07` | 上传校验、删除、点赞、分页和视频服务基础行为 | 7 | 通过 |
| `VIDEO-API-01` 至 `VIDEO-API-03` | 完整服务 Controller 的删除、推荐和详情响应 | 3 | 通过 |

### 7.3 UC-03 H2 API 集成测试

测试文件：`video-service/src/test/java/com/example/video/recommendation/RecommendationApiH2Test.java`

| 编号 | 测试内容 | 结果 |
| --- | --- | --- |
| `API-TC-03-01` | 游客只获取公开视频并按热度排序 | 通过 |
| `API-TC-03-02` | 关注作者视频优先并返回 `following=true` | 通过 |
| `API-TC-03-03` | 兴趣标签匹配视频优先 | 通过 |
| `API-TC-03-04` | 本地观看历史使已观看视频降权 | 通过 |
| `API-TC-03-05` | 分类和关键词同时筛选 | 通过 |
| `API-TC-03-06` | 分页返回排序后的指定页 | 通过 |
| `API-TC-03-07` | 无匹配结果返回成功和空数组 | 通过 |
| `API-TC-03-08` | 非法页码返回 HTTP 400 | 通过 |
| `API-TC-03-09` | 非法用户标识返回 HTTP 400 | 通过 |

## 8. 真实 MySQL API 测试明细

真实测试链路如下：

```text
Node.js API 测试脚本
  -> http://127.0.0.1:8082
  -> video-service Docker 容器
  -> VideoMapper / MyBatis Plus
  -> MySQL 8.4 Docker 容器中的 video_db
  -> JSON 响应
  -> Node.js assert 断言
```

| 编号 | 原始输入 | 预期输出 | 实际结果 |
| --- | --- | --- | --- |
| `LIVE-API-TC-03-01` | `GET /actuator/health` | HTTP 200，`status=UP` | 通过 |
| `LIVE-API-TC-03-02` | `GET /api/videos/recommend` | 只返回 3 条公开视频；按 `930301,930302,930303` 排序 | 通过 |
| `LIVE-API-TC-03-03` | 同上，头部 `X-User-Id: 930001` | MySQL 中已观看的 `930301` 被降权并排到最后 | 通过 |
| `LIVE-API-TC-03-04` | `categoryId=3&keyword=music` | 只返回视频 `930301` | 通过 |
| `LIVE-API-TC-03-05` | `page=2&pageSize=1` | 返回排序后的第二条视频 `930302` | 通过 |
| `LIVE-API-TC-03-06` | `keyword=not-found` | HTTP 200，`data=[]` | 通过 |
| `LIVE-API-TC-03-07` | `page=abc` | HTTP 400，业务码 400 | 通过 |
| `LIVE-API-TC-03-08` | 头部 `X-User-Id: guest` | HTTP 400，业务码 400 | 通过 |

测试脚本将每项用例的请求方法、路径、请求头、HTTP 状态和完整 JSON 响应写入：

```text
video-service/target/api-test-results/api-results.json
```

该目录属于运行生成物，不提交到 Git；流水线通过 Artifact 保存原始报告。

## 9. Playwright 浏览器端到端测试

当前 `microservice` 分支的浏览器 E2E 使用前端 `5173` 和微服务网关 `8080`，由网关将推荐请求转发到 `video-service:8082`：

```text
Playwright Chromium
  -> Vue 前端 http://127.0.0.1:5173
  -> 单体后端 http://127.0.0.1:8080
  -> 临时 MySQL 8.4 容器（主机端口 3307）
  -> 页面和网络响应断言
```

| 编号 | 用户操作 | 主要断言 | 结果 |
| --- | --- | --- | --- |
| `E2E-TC-03-01` | 游客打开首页 | 展示热门和普通公开视频；不展示私密视频；热门视频排序靠前 | 通过 |
| `E2E-TC-03-02` | 输入“校园音乐”并搜索 | 推荐接口响应成功；页面仅展示校园音乐视频 | 通过 |
| `E2E-TC-03-03` | 使用固定测试账号登录 | 请求携带 `X-User-Id: 930001`；关注作者视频排在首位 | 通过 |
| `E2E-TC-03-04` | 搜索不存在的内容 | 视频卡片为 0；页面展示空结果提示 | 通过 |
| `E2E-TC-03-05` | 点击推荐卡片后返回 | 进入视频页并显示正确标题；可返回首页 | 通过 |

测试文件和固定数据：

```text
frontend/demo/e2e/recommendation.spec.js
frontend/demo/e2e/fixtures/recommendation.sql
```

本地原始报告：

```text
frontend/demo/playwright-report/index.html
frontend/demo/test-results/uc03-playwright-junit.xml
```

HTML、JUnit、失败截图、录像和 trace 均属于运行生成物，不提交到 Git；流水线通过 Artifact 保存。该结果证明 UC-03 在现有单体系统中的完整页面流程可用，不代表前端已经经由网关调用拆分后的 `video-service`。

## 10. 缺陷与处理记录

| 编号 | 发现阶段 | 现象 | 原因 | 处理 | 复测结果 |
| --- | --- | --- | --- | --- | --- |
| `DEFECT-UC03-01` | 首次 Docker 启动 | 服务启动失败，提示不存在 `UserPreferenceClient` Bean | 原降级实现依赖 `ConditionalOnMissingBean`，在当前组件扫描顺序下未注册默认实现；H2 测试使用模拟 Bean，未暴露该问题 | 增加 `user-service.enabled` 显式开关；默认注册 `NoopUserPreferenceClient`，启用时注册 HTTP 客户端 | 镜像重新构建成功；容器正常启动；健康检查通过；8 项真实 API 测试全部通过 |

最终测试中失败数为 0。上表保留的是测试过程中发现并修复的问题，不计入最终通过率。

## 11. CI 执行与失败阻断

现有 `.github/workflows/microservices-ci.yml` 负责当前三个微服务的测试、Compose 联调、镜像构建和可选 Kubernetes 部署；旧 `.github/workflows/ci.yml` 仅属于历史单体/旧分支工作流。

- `feature/video-service`
- `feature/video-service-*`
- 面向 `feature/video-service` 或 `main` 的 Pull Request

作业执行顺序：

```text
取代码
  -> 配置 Java 和 Node.js
  -> 运行 24 项整合分支 JUnit 测试
  -> 构建 video-service 镜像并启动 MySQL + MinIO
  -> 运行 8 项真实 API 测试
  -> 保存 Surefire、API JSON 和容器日志
  -> 清理隔离环境
```

步骤使用默认的成功依赖关系。JUnit、镜像构建、容器启动或真实 API 断言任一步失败，后续正常步骤不会继续执行；报告上传和环境清理使用 `if: always()`，因此失败证据仍会保留。

`uc03-browser-e2e` 独立启动单体后端和测试基础设施，导入固定数据，只执行 5 项 UC-03 Playwright 用例。前端构建、浏览器安装或任一页面断言失败时，该作业失败，并保留 Playwright HTML、JUnit、失败截图、录像、trace 和后端日志。

旧 UC-03 分支的 CI 运行记录不代表当前版本结果；当前 `microservice` 分支尚未推送，也未创建 PR。推送后应查看 `microservices-ci.yml` 的实际运行结果，再补充 CI 链接。

## 12. 需求与测试追溯

| 需求/路径 | 单元/H2 测试 | 真实 API | 浏览器 E2E |
| --- | --- | --- | --- |
| `REQ-02 / UC-03` 游客推荐 | `UNIT-TC-03-01`、`03-19`；`API-TC-03-01` | `LIVE-API-TC-03-02` | `E2E-TC-03-01` |
| 登录用户个性化 | `UNIT-TC-03-04`、`03-07`、`03-08`；`API-TC-03-02`、`03-03` | Compose 网关联调已验证；user-service 不可用时有降级证据 | `E2E-TC-03-03`（微服务网关链路） |
| 观看历史降权 | `UNIT-TC-03-09`；`API-TC-03-04` | `LIVE-API-TC-03-03` | - |
| 分类和关键词筛选 | `UNIT-TC-03-02`、`03-10`；`API-TC-03-05` | `LIVE-API-TC-03-04` | `E2E-TC-03-02` |
| 排序和分页 | `UNIT-TC-03-03`、`03-11`、`03-14` 至 `03-16`；`API-TC-03-06` | `LIVE-API-TC-03-05` | `E2E-TC-03-01` |
| 空结果 | `UNIT-TC-03-05`、`03-17`；`API-TC-03-07` | `LIVE-API-TC-03-06` | `E2E-TC-03-04` |
| 推荐页到播放页 | - | - | `E2E-TC-03-05` |
| 非法参数 | `UNIT-TC-03-23`、`03-24`；`API-TC-03-08`、`03-09` | `LIVE-API-TC-03-07`、`03-08` | - |
| 服务健康状态 | - | `LIVE-API-TC-03-01` | - |

## 13. 复现步骤

### 13.1 JUnit 与 H2 API 测试

```powershell
cd video-service
mvn -B -ntp test
```

Surefire 原始报告位于：

```text
video-service/target/surefire-reports/
```

### 13.2 Docker 与真实 MySQL API 测试

```powershell
cd video-service
$env:VIDEO_SERVICE_DB_PASSWORD = "<本机测试密码>"
docker compose -f compose.test.yml up -d --build
node scripts/api-smoke-test.mjs
docker compose -f compose.test.yml down -v
```

不得把实际密码、Token 或密钥写入仓库。

### 13.3 Playwright 浏览器 E2E

准备测试数据库并启动单体后端后，在前端目录执行：

```powershell
cd frontend/demo
npm ci
npx playwright install chromium
npm run test:e2e:uc03
```

固定数据必须先导入独立测试数据库。不要在包含真实业务数据的数据库中执行 `recommendation.sql`，因为该脚本会清理并重建指定编号范围内的测试记录。

## 14. 结论与后续工作

在当前 `microservice` 分支范围内，UC-03 推荐业务规则、Controller 参数处理、MyBatis 数据访问、真实 MySQL API 和网关浏览器 E2E 均已验证。video-service Maven 测试为 `24/24`，真实推荐 API 为 `8/8`，UC-03 浏览器 E2E 为 `5/5`，失败、错误和跳过均为 0。

本报告同时关联了微服务阶段的 Compose、Kubernetes、故障和性能证据。仍未完成的内容必须在答辩中主动说明：本机缺少 FFmpeg，SRS 浏览器推流专项无法复验。单体与微服务已形成同机同参数、各 3 次重复的性能对照，汇总见 `benchmark-results/性能对照汇总-20260903.md`。HPA 已在 `evidence/hpa/20260903-031813/` 保存清晰的扩容与缩容输出。
